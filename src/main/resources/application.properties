spring.application.name=BookWhiz

# ===============================
# Server
# ===============================
server.port=8080

# ===============================
# Database (MySQL)
# ===============================
spring.datasource.url=jdbc:mysql://bg56ddf3oiyljsjik3ms-mysql.services.clever-cloud.com:3306/bg56ddf3oiyljsjik3ms
spring.datasource.username=u3znax4qicfllaxz
spring.datasource.password=9uWO5MPPxpcdzEv0ALWP
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ===============================
# JWT
# ===============================
jwt.secret=bookwhiz_super_secure_jwt_secret_key_256_bits_long
jwt.expiration=86400000

# ===============================
# Logging
# ===============================
logging.level.org.springframework.security=DEBUG
spring.cache.type=redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.url=${SPRING_DATA_REDIS_URL:redis://localhost:6379}



# ── Add these lines to your application.properties ──────────

# Base URL of your backend (used to build poster URLs returned to frontend)
app.base-url=http://localhost:8080

# Directory where uploaded poster images will be stored
# Path is relative to where you run the Spring Boot app (project root)
app.upload.dir=uploads/posters

# Allow multipart uploads up to 10 MB per file / 20 MB per request
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

# Directory where uploaded poster images will be stored
# Path is relative to where you run the Spring Boot app (project root)
app.upload.dir=uploads/posters

# Allow multipart uploads up to 10 MB per file / 20 MB per request
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=iamvigneshofficial@gmail.com
spring.mail.password=weamamcrjtbuxsmep  
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
bookwhiz.mail.from=BookWhiz <iamvigneshofficial@gmail.com>